Editor: Neovim (use `+` register for yanking)
Python tooling: uv
Environments: direnv (verify `.envrc` before exporting vars)
New Python projects: execute local `uvnew` function

Never use rm to delete files. Always use trash instead.
When using mv, always use -n flag to prevent silently overwriting existing files.
